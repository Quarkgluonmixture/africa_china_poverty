# Core deep-learning stack (PyTorch rebuild).
# NOTE on PyTorch: install the build that matches your GPU/arch, e.g.
#   NVIDIA Blackwell (GB10, sm_121, ARM64):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   Generic CUDA 12.x x86_64:
#     pip install torch torchvision
# Versions below are what this project was developed/trained against.
torch>=2.7            # 2.11.0+cu128 used on the DGX Spark GB10
torchvision>=0.22
timm>=1.0.0           # pretrained backbones (ResNet-50, ConvNeXt, ViT)

# Data / numerics / plotting
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3     # stratified splits, metrics
scipy>=1.11
matplotlib>=3.7
Pillow>=10.0
pyyaml>=6.0
tqdm>=4.65

# Optional: satellite-image download pipeline (scripts/data_prep/)
# earthengine-api>=0.1.380
# requests
